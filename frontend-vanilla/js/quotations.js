import { apiClient } from './api_client.js';

// 이 파일은 향후 견적관리 기능 개발 시 사용됩니다.
// 현재는 새로운 UI 레이아웃에 맞춰 기본 구조만 설정합니다.

document.addEventListener('DOMContentLoaded', () => {
    console.log("quotations.js loaded");
    // 예: initQuotationPage();
});

function initQuotationPage() {
    // 향후 견적 목록 로딩 및 이벤트 처리 로직이 여기에 들어옵니다.
    // const quotationListContainer = document.getElementById('quotation-list');
    // ...
}
